pre-commit:
  commands:
    i18n-tasks:
      run: bin/i18n-tasks health

    rubocop:
      glob: "*.rb"
      run: bin/rubocop --force-exclusion {staged_files}
      stage_fixed: true

    erb_lint:
      glob: "*.erb"
      run: bundle exec erb_lint {staged_files}

    slim_lint:
      glob: "*.slim"
      run: bin/slim-lint {staged_files}

    stylelint:
      glob: "*.{css,scss}"
      run: yarn stylelint {staged_files}

pre-push:
  commands:
    brakeman:
      run: bin/brakeman --no-pager

    bundler-audit:
      run: bin/bundler-audit

    i18n-tasks:
      run: bin/i18n-tasks health

    yarn-audit:
      run: yarn npm audit

    rspec:
      run: bin/rspec --format progress
