<%# locals: (inspection:, items_grouped:, defects:) %>
<div class="report-pdf">
  <%= render "header", inspection: inspection %>

  <%= render "checklist", inspection: inspection, items_grouped: items_grouped %>

  <%= render "defects_summary", defects: defects %>

  <%= render "signature", inspection: inspection %>

  <%= render "footer" %>
</div>
