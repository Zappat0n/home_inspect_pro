<div class="max-w-3xl mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-8">
    <%= t("inspections.new.title") %>
  </h1>

  <%= render(
        partial: "form",
        locals: {
          inspection: inspection,
          templates: templates,
        },
      ) %>
</div>
