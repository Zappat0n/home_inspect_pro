<div class="mb-10">
<%= form_with(model: template, url: inspection_template_path(template), method: :patch,
      data: { controller: "auto-submit", turbo_stream: true }) do |form| %>
  <% if template.errors.any? %>
    <div data-testid="template-form-errors" class="bg-red-50 border border-red-200 rounded p-4 mb-6">
      <h2 class="text-red-800 font-medium mb-2"><%= t("errors.messages.not_saved", count: template.errors.count, resource: template.model_name.human.downcase) %></h2>
      <ul class="list-disc list-inside text-sm text-red-700">
        <% template.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= form.text_field :name,
          class: "w-full border border-gray-300 rounded px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-transparent",
          data: { action: "blur->auto-submit#submit" },
          "data-testid": "template-name-input" %>
  </div>
<% end %>
</div>
