<p><%= t("devise.views.mailers.unlock.hello", email: @resource.email) %></p>

<p><%= t("devise.views.mailers.unlock.account_locked") %></p>

<p><%= t("devise.views.mailers.unlock.click_to_unlock") %></p>

<p><%= link_to t("devise.views.mailers.unlock.unlock_link"), unlock_url(@resource, unlock_token: @token) %></p>
