<p><%= t("devise.views.mailers.confirmation.welcome", email: @email) %></p>

<p><%= t("devise.views.mailers.confirmation.confirm_through_link") %></p>

<p><%= link_to t("devise.views.mailers.confirmation.confirm_link"), confirmation_url(@resource, confirmation_token: @token) %></p>
